<!DOCTYPE html>
<html lang="pl">
<head><%- include('partials/head', { pageTitle: 'Edytuj skuter' }) %></head>
<body>
<%- include('partials/navbar') %>
<main class="page-center">
  <div class="card card--wide">
    <h1 class="card__title">Edytuj skuter</h1>

    <% if (errors && errors.length) { %>
      <ul class="error-list">
        <% errors.forEach(e => { %><li><%= e %></li><% }) %>
      </ul>
    <% } %>

    <form method="POST" action="/scooters/edit/<%= scooter.id %>" novalidate>
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <div class="form-row">
        <div class="form-group">
          <label for="brand">Marka <span class="required">*</span></label>
          <input type="text" id="brand" name="brand"
            value="<%= (old && old.brand) || scooter.brand %>"
            maxlength="100" required>
        </div>
        <div class="form-group">
          <label for="model">Model <span class="required">*</span></label>
          <input type="text" id="model" name="model"
            value="<%= (old && old.model) || scooter.model %>"
            maxlength="100" required>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="engine_capacity">Pojemność silnika (cc)</label>
          <input type="number" id="engine_capacity" name="engine_capacity"
            value="<%= (old && old.engine_capacity) || scooter.engine_capacity || '' %>"
            min="1" max="2000">
        </div>
        <div class="form-group">
          <label for="year">Rok produkcji</label>
          <input type="number" id="year" name="year"
            value="<%= (old && old.year) || scooter.year || '' %>"
            min="1900" max="<%= new Date().getFullYear() + 1 %>">
        </div>
      </div>

      <div class="form-group">
        <label for="description">Opis</label>
        <textarea id="description" name="description"><%= (old && old.description !== undefined ? old.description : scooter.description) || '' %></textarea>
      </div>

      <div class="form-actions">
        <a href="/dashboard" class="btn btn--ghost">Anuluj</a>
        <button type="submit" class="btn btn--primary">Zapisz zmiany</button>
      </div>
    </form>
  </div>
</main>
</body>
</html>
