<!DOCTYPE html>
<html lang="pl">
<head><%- include('partials/head', { pageTitle: 'Strona główna' }) %></head>
<body>
<%- include('partials/navbar') %>
<main class="hero">
  <div class="hero__inner">
    <p class="hero__eyebrow">Twoja baza danych</p>
    <h1 class="hero__title">Skuter&shy;Base</h1>
    <p class="hero__sub">Zarządzaj swoją kolekcją skuterów w jednym miejscu.</p>
    <div class="hero__actions">
      <% if (userId) { %>
        <a href="/dashboard" class="btn btn--primary btn--lg">Przejdź do dashboardu</a>
      <% } else { %>
        <a href="/register" class="btn btn--primary btn--lg">Zacznij teraz</a>
        <a href="/login"    class="btn btn--outline btn--lg">Mam już konto</a>
      <% } %>
    </div>
  </div>
</main>
</body>
</html>
